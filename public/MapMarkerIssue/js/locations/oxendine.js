function Oxendine (){
    import {collection} from '../collection.js';
    let number = 0;
    for(let x = 0; x < collection.length; x++){
        if(collection[x][0] = "Oxendine Science Building"){
            number = x;
            break;
        }
    }
    console.log(number);
    return number;
}
    
